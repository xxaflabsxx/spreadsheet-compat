{
  "function": "SHEET",
  "cases": [
    {
      "id": "SHEET_returns_positive_index",
      "formula": "=SHEET()>=1",
      "description": "SHEET() returns the current sheet's 1-based index; the absolute number depends on workbook structure (our harness batches many test sheets into one workbook), so we assert the invariant instead",
      "expected": true
    }
  ]
}

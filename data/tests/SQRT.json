{
  "function": "SQRT",
  "cases": [
    {
      "id": "SQRT_basic",
      "formula": "=SQRT(16)",
      "description": "Basic perfect-square root",
      "expected": 4
    },
    {
      "id": "SQRT_zero",
      "formula": "=SQRT(0)",
      "description": "Square root of zero is zero",
      "expected": 0
    },
    {
      "id": "SQRT_negative_returns_num_error",
      "formula": "=SQRT(-16)",
      "description": "Negative input has no real square root -> #NUM!",
      "expected": "#NUM!",
      "expected_note": "Microsoft's SQRT docs state verbatim: 'If number is negative, SQRT returns the #NUM! error value' -- https://support.microsoft.com/en-US/Excel/functions/sqrt-function"
    },
    {
      "id": "SQRT_documented_abs_workaround",
      "formula": "=SQRT(ABS(-16))",
      "description": "Microsoft's own SQRT documentation demonstrates wrapping a possibly-negative argument in ABS as the standard workaround to avoid #NUM!",
      "expected": 4,
      "expected_note": "Same doc page above shows an example using '=SQRT(ABS(A2))' specifically to sidestep the negative-input #NUM! case"
    }
  ]
}

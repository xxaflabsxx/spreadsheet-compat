{
  "function": "RANDBETWEEN",
  "cases": [
    {
      "id": "RANDBETWEEN_exists_bounds",
      "formula": "=RANDBETWEEN(1,10)",
      "description": "Existence probe: confirm recognized and result plausible (integer 1..10)",
      "expected": null,
      "expected_note": "Non-deterministic by design; harness should only check 1<=value<=10 and absence of #NAME?"
    },
    {
      "id": "RANDBETWEEN_degenerate_equal_bounds",
      "formula": "=RANDBETWEEN(5,5)",
      "description": "When bottom==top the result is deterministic",
      "expected": 5
    },
    {
      "id": "RANDBETWEEN_negative_range",
      "formula": "=RANDBETWEEN(-5,-1)",
      "description": "Existence probe with an all-negative range",
      "expected": null,
      "expected_note": "Non-deterministic; harness should only check -5<=value<=-1"
    }
  ]
}

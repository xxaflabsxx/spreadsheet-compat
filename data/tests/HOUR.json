{
  "function": "HOUR",
  "cases": [
    {
      "id": "HOUR_of_fractional_day_noon",
      "formula": "=HOUR(0.5)",
      "description": "HOUR applied to a bare fractional-day serial (no date part) -- 0.5 of a day is exactly noon",
      "expected": 12
    },
    {
      "id": "HOUR_of_time_value",
      "formula": "=HOUR(TIME(15,45,10))",
      "description": "HOUR applied to a TIME()-constructed value",
      "expected": 15
    },
    {
      "id": "HOUR_of_date_plus_time_serial",
      "formula": "=HOUR(45366.75)",
      "description": "HOUR applied to a combined date+time serial extracts only the time-of-day part",
      "expected": 18,
      "expected_note": "0.75 of a day = 18:00"
    }
  ]
}

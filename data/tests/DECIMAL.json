{
  "function": "DECIMAL",
  "cases": [
    {
      "id": "DECIMAL_basic",
      "formula": "=DECIMAL(\"FF\",16)",
      "description": "Parse hexadecimal text back to a number",
      "expected": 255
    }
  ]
}
{
  "function": "AREAS",
  "cases": [
    {
      "id": "AREAS_single_range",
      "formula": "=AREAS(B2:D4)",
      "description": "Microsoft's documented worked example: a single contiguous range is one area",
      "expected": 1,
      "expected_note": "Microsoft's page publishes this example's result as 1. Follows directly from the documented definition: \"An area is a range of contiguous cells or a single cell\""
    },
    {
      "id": "AREAS_union_three_areas",
      "formula": "=AREAS((B2:D4,E5,F6:I9))",
      "description": "Microsoft's documented worked example: a union of three references, wrapped in the extra parentheses the page requires",
      "expected": 3,
      "expected_note": "Microsoft's page publishes this example's result as 3. The documentation specifies the extra set of parentheses so Excel does not read the commas as argument separators; the union then contains three areas (a 3x3 range, a single cell, and a 4x4 range)"
    },
    {
      "id": "AREAS_intersection_one_area",
      "formula": "=AREAS(B2:D4 B2)",
      "description": "Microsoft's documented worked example: the space intersection operator yields a single area",
      "expected": 1,
      "expected_note": "Microsoft's page publishes this example's result as 1. The space operator intersects B2:D4 with B2, and the single-cell result is one contiguous area"
    },
    {
      "id": "AREAS_single_cell",
      "formula": "=AREAS(B2)",
      "description": "A single cell reference",
      "expected": 1,
      "expected_note": "Follows directly from the documented definition that \"an area is a range of contiguous cells or a single cell\", so a lone cell reference is exactly one area"
    }
  ]
}

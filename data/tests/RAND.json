{
  "function": "RAND",
  "cases": [
    {
      "id": "RAND_exists_and_in_unit_range",
      "formula": "=RAND()",
      "description": "Existence probe only: RAND() is non-deterministic; we just confirm the engine recognizes the function (not #NAME?) and returns a plausible value",
      "expected": null,
      "expected_note": "Non-deterministic by design; harness should only check 0<=value<1 and absence of #NAME?"
    }
  ]
}

{
  "function": "NA",
  "cases": [
    {
      "id": "NA_direct",
      "formula": "=NA()",
      "description": "NA() returns the #N/A error value itself",
      "expected": "#N/A"
    },
    {
      "id": "NA_isna_true",
      "formula": "=ISNA(NA())",
      "description": "ISNA(NA()) must be TRUE -- confirms NA() genuinely produces the #N/A error",
      "expected": true
    },
    {
      "id": "NA_propagates_through_arithmetic",
      "formula": "=NA()+1",
      "description": "The #N/A error propagates through arithmetic like any other error value",
      "expected": "#N/A"
    },
    {
      "id": "NA_isnumber_false",
      "formula": "=ISNUMBER(NA())",
      "description": "#N/A is an error, not a number -> FALSE",
      "expected": false
    }
  ]
}

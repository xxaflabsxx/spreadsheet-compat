{
  "slug": "vlookup-vs-xlookup",
  "title": "VLOOKUP vs XLOOKUP: which should you use?",
  "meta_desc": "VLOOKUP vs XLOOKUP compared: syntax, left-lookups, defaults, error handling, and real compatibility across Excel, Google Sheets, and LibreOffice (XLOOKUP needs 24.8+).",
  "funcs": ["VLOOKUP", "XLOOKUP"],
  "intro": "XLOOKUP is VLOOKUP's modern replacement — but VLOOKUP still wins on one thing: it runs everywhere, including old Excel versions and every LibreOffice release. Here's the practical difference, with compatibility verified by actually executing both.",
  "table": [
    {"aspect": "Look left of the key column", "cols": ["No — the key must be the first column", "Yes — lookup and return ranges are independent"]},
    {"aspect": "Default match mode", "cols": ["Approximate (a classic silent-error trap — always pass FALSE)", "Exact (the safe default)"]},
    {"aspect": "Built-in if-not-found", "cols": ["No — wrap in IFERROR", "Yes — 4th argument"]},
    {"aspect": "Column referenced by", "cols": ["Number (breaks when columns are inserted)", "Range (survives column inserts)"]},
    {"aspect": "Search from the end", "cols": ["No", "Yes (search mode -1)"]},
    {"aspect": "Return multiple columns at once", "cols": ["No", "Yes — return range can be several columns (spills)"]},
    {"aspect": "Works in old Excel (2019 and earlier)", "cols": ["Yes", "No — Excel 2021+ / 365 only"]},
    {"aspect": "LibreOffice support (executed result)", "cols": ["All versions", "24.8+ only — returns #NAME? in 24.2"]}
  ],
  "when": [
    {"func": "VLOOKUP", "use_when": "The file must open correctly for users on old Excel or LibreOffice 24.2, or you're maintaining an existing sheet that already uses it (with FALSE as the 4th argument)."},
    {"func": "XLOOKUP", "use_when": "Everyone's on Excel 2021+/365, current Google Sheets, or LibreOffice 24.8+ — exact-match default, left-lookups, and the built-in not-found argument remove VLOOKUP's three classic bugs."}
  ],
  "compat_note": "We executed both in real LibreOffice: VLOOKUP works in every release we test; XLOOKUP first works in 24.8 (verified #NAME? in 24.2). In Google Sheets both are available.",
  "examples": [
    {"label": "VLOOKUP (exact match — always pass FALSE)", "formula": "=VLOOKUP(\"cherry\",A2:B10,2,FALSE)"},
    {"label": "XLOOKUP equivalent with a not-found default", "formula": "=XLOOKUP(\"cherry\",A2:A10,B2:B10,\"Not found\")"}
  ]
}

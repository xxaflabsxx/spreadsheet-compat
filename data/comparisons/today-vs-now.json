{
  "slug": "today-vs-now",
  "title": "TODAY vs NOW: date, or date plus time?",
  "meta_desc": "TODAY() vs NOW() in spreadsheets: midnight semantics, why date comparisons break with NOW, volatility, and freezing timestamps. Executed in LibreOffice Calc; Excel and Google Sheets behavior per official docs.",
  "funcs": [
    "TODAY",
    "NOW"
  ],
  "intro": "TODAY() is midnight at the start of today; NOW() is this exact second. The fractional day NOW carries is what quietly breaks date comparisons — and neither function is the right tool when you need a timestamp that doesn't change.",
  "table": [
    {
      "aspect": "Returns",
      "cols": [
        "Today's date (time = 00:00)",
        "Date AND time-of-day fraction"
      ]
    },
    {
      "aspect": "A2=TODAY() when A2 is today's date",
      "cols": [
        "TRUE",
        "FALSE for NOW() — the time fraction spoils equality"
      ]
    },
    {
      "aspect": "Updates",
      "cols": [
        "On any recalculation (volatile)",
        "Same — every recalc, so it creeps forward"
      ]
    },
    {
      "aspect": "Strip to a date",
      "cols": [
        "Already a date",
        "INT(NOW()) or TRUNC(NOW())"
      ]
    },
    {
      "aspect": "Compatibility",
      "cols": [
        "Universal",
        "Universal (verified in every version we test)"
      ]
    }
  ],
  "when": [
    {
      "func": "TODAY",
      "use_when": "Deadlines, ages, overdue flags — anything comparing DATES. Using NOW in a date comparison is the classic off-by-a-fraction bug."
    },
    {
      "func": "NOW",
      "use_when": "Displaying a live clock or computing hours-since. For a PERMANENT timestamp, use neither: press Ctrl+; (date) or Ctrl+Shift+; (time) to stamp a static value that won't update tomorrow."
    }
  ],
  "compat_note": "Both are volatile and behave identically in every LibreOffice build and in the Google Sheets run we execute (desktop Excel per Microsoft's docs — we do not run desktop Excel) (our harness verifies NOW()>=TODAY() and NOW()-NOW()=0 as executed facts). Volatility has a cost: thousands of NOW/TODAY cells force full recalcs — reference one helper cell instead of repeating the call per row. Excel for the web is a separate application with its own calculation engine, and that one we do execute: all 6 corpus cases for these functions matched the documented values there (recalculated on OneDrive, 2026-09-01); the per-case values are on the individual function pages.",
  "examples": [
    {
      "label": "Overdue test (correct — dates compared)",
      "formula": "=A2<TODAY()"
    },
    {
      "label": "Hours since a timestamp",
      "formula": "=(NOW()-A2)*24"
    },
    {
      "label": "Today from NOW",
      "formula": "=INT(NOW())"
    }
  ]
}

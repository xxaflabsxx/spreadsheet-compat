{
  "slug": "count-words-in-a-cell",
  "title": "How to count the number of words in a cell",
  "task": "Count how many words are in a text string, ignoring extra spaces.",
  "keywords": ["count words in a cell excel", "word count google sheets formula", "how many words in a cell", "count spaces plus one"],
  "solutions": {
    "excel": {"formula": "=LEN(TRIM(A2))-LEN(SUBSTITUTE(TRIM(A2),\" \",\"\"))+1", "note": "Word count = number of spaces + 1, after trimming."},
    "google_sheets": {"formula": "=LEN(TRIM(A2))-LEN(SUBSTITUTE(TRIM(A2),\" \",\"\"))+1", "note": "Identical."},
    "libreoffice": {"formula": "=LEN(TRIM(A2))-LEN(SUBSTITUTE(TRIM(A2),\" \",\"\"))+1", "note": "Identical."}
  },
  "verify": {"setup_cells": {"A2": "the quick brown fox"}, "formula": "=LEN(TRIM(A2))-LEN(SUBSTITUTE(TRIM(A2),\" \",\"\"))+1", "expected": 4},
  "explanation": "TRIM removes extra spaces. The word count equals the number of single spaces between words plus one. SUBSTITUTE(...,\" \",\"\") deletes the spaces, and the drop in length tells you how many there were. 'the quick brown fox' has 3 spaces, so 4 words. (An empty cell would wrongly return 1; wrap in IF(A2=\"\",0,...) if that matters.)"
}

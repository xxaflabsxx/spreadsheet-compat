{
  "slug": "convert-a-date-to-text",
  "title": "How to convert a date to text",
  "task": "Turn a real date into a text string in a format you control — for filenames, labels, exports, or joining.",
  "keywords": [
    "convert date to text excel",
    "date to string formula",
    "text function date format",
    "date as text google sheets"
  ],
  "solutions": {
    "excel": {
      "formula": "=TEXT(A2,\"yyyy-mm-dd\")",
      "note": "The format code controls the output: \"mmmm d, yyyy\" -> July 21, 2026; \"ddd\" -> Tue."
    },
    "google_sheets": {
      "formula": "=TEXT(A2,\"yyyy-mm-dd\")",
      "note": "Identical."
    },
    "libreoffice": {
      "formula": "=TEXT(A2,\"YYYY-MM-DD\")",
      "note": "Identical (format codes are case-insensitive here)."
    }
  },
  "verify": {
    "setup_cells": {},
    "formula": "=TEXT(DATE(2026,7,21),\"YYYY-MM-DD\")",
    "expected": "2026-07-21"
  },
  "explanation": "TEXT renders the date's underlying serial number as a string using the format code you give it, so July 21, 2026 becomes the text \"2026-07-21\". Unlike changing the cell format (which only affects display), the result here is genuinely text — it can be concatenated, used in a filename, or exported, but it won't sort or calculate as a date anymore. Pick the format to match your need: \"yyyymmdd\" for sortable filenames, \"mmmm yyyy\" for report headers. The reverse (text back to a real date) is DATEVALUE."
}

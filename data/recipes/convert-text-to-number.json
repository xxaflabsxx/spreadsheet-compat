{
  "slug": "convert-text-to-number",
  "title": "How to convert text that looks like a number into a real number",
  "task": "Turn numbers stored as text (left-aligned, won't sum) into actual numeric values.",
  "keywords": [
    "convert text to number excel",
    "value function google sheets",
    "text to number formula",
    "numbers stored as text"
  ],
  "solutions": {
    "excel": {
      "formula": "=VALUE(A2)",
      "note": "Or a quick trick: =A2*1 or =A2+0."
    },
    "google_sheets": {
      "formula": "=VALUE(A2)",
      "note": "Identical."
    },
    "libreoffice": {
      "formula": "=VALUE(A2)",
      "note": "Identical."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": "123"
    },
    "formula": "=VALUE(A2)",
    "expected": 123
  },
  "explanation": "VALUE() parses a text string into a number so it can be summed and compared. =A2*1 does the same by forcing arithmetic. The reverse (number to text) is TEXT()."
}
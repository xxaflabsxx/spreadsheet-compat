{
  "slug": "absolute-vs-relative-references",
  "title": "How to lock a cell reference (absolute vs relative, the $ sign)",
  "task": "Stop a reference from shifting when you fill a formula down or across — the dollar-sign rule.",
  "keywords": [
    "lock cell reference excel",
    "absolute vs relative reference",
    "dollar sign in formula",
    "f4 absolute reference google sheets"
  ],
  "solutions": {
    "excel": {
      "formula": "=A2*$B$1",
      "note": "$ locks what follows. $B$1 = fully locked; B$1 = row locked; $B1 = column locked. F4 cycles them."
    },
    "google_sheets": {
      "formula": "=A2*$B$1",
      "note": "Identical; F4 also cycles the $ modes."
    },
    "libreoffice": {
      "formula": "=A2*$B$1",
      "note": "Identical; Shift+F4 cycles the modes."
    }
  },
  "verify": {
    "setup_cells": {
      "B1": 10,
      "A2": 5
    },
    "formula": "=A2*$B$1",
    "expected": 50
  },
  "explanation": "A plain reference like A2 is RELATIVE — fill the formula down and it becomes A3, A4, shifting with the row. A $ freezes that part: $B$1 always points at B1 no matter where you copy it (5 x 10 = 50 here, and every filled row keeps multiplying by B1). The dollar attaches to whatever follows it, so you can lock just the column ($B1), just the row (B$1), or both ($B$1) — the mixed forms are the trick behind multiplication tables and single-rate calculations. Press F4 (Shift+F4 in LibreOffice) on a highlighted reference to cycle through the four modes."
}

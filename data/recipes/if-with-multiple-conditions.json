{
  "slug": "if-with-multiple-conditions",
  "title": "How to write an IF with multiple conditions (AND / OR)",
  "task": "Return one result only when several conditions are all true (or when any is true).",
  "keywords": ["if with multiple conditions excel", "if and formula google sheets", "if two conditions", "nested if and or"],
  "solutions": {
    "excel": {"formula": "=IF(AND(A2>=60,B2>=60),\"Pass\",\"Fail\")", "note": "AND requires all conditions true; swap in OR() to require any one."},
    "google_sheets": {"formula": "=IF(AND(A2>=60,B2>=60),\"Pass\",\"Fail\")", "note": "Identical."},
    "libreoffice": {"formula": "=IF(AND(A2>=60,B2>=60),\"Pass\",\"Fail\")", "note": "Identical."}
  },
  "verify": {"setup_cells": {"A2": 75, "B2": 80}, "formula": "=IF(AND(A2>=60,B2>=60),\"Pass\",\"Fail\")", "expected": "Pass"},
  "explanation": "AND(cond1, cond2, ...) is TRUE only when every condition is true, so IF returns 'Pass' only when both scores are at least 60. Here 75 and 80 both pass. Replace AND with OR to return 'Pass' when at least one condition is met."
}

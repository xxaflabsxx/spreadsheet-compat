{
  "slug": "find-the-value-corresponding-to-the-max",
  "title": "How to find the label that goes with the highest value",
  "task": "Not the biggest number itself, but WHICH item it belongs to — top salesperson, best-selling product, peak day.",
  "keywords": [
    "find value corresponding to max excel",
    "index match max",
    "which item has highest value",
    "lookup max value name google sheets"
  ],
  "solutions": {
    "excel": {
      "formula": "=INDEX(A2:A6,MATCH(MAX(B2:B6),B2:B6,0))",
      "note": "MAX finds the biggest number; MATCH locates its row; INDEX returns the matching label. Use MIN for the lowest."
    },
    "google_sheets": {
      "formula": "=INDEX(A2:A6,MATCH(MAX(B2:B6),B2:B6,0))",
      "note": "Identical; =XLOOKUP(MAX(B2:B6),B2:B6,A2:A6) also works."
    },
    "libreoffice": {
      "formula": "=INDEX(A2:A6,MATCH(MAX(B2:B6),B2:B6,0))",
      "note": "Identical."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": "Ada", "B2": 40,
      "A3": "Bob", "B3": 95,
      "A4": "Cy", "B4": 70,
      "A5": "Dot", "B5": 60
    },
    "formula": "=INDEX(A2:A5,MATCH(MAX(B2:B5),B2:B5,0))",
    "expected": "Bob"
  },
  "explanation": "Three steps working inside-out: MAX(B2:B5) finds the largest number (95), MATCH locates which row that value sits in (row 2 of the range), and INDEX returns the label from column A on that row — \"Bob\". Swap MAX for MIN to get the label of the smallest value, or use MAXIFS/MINIFS inside for a conditional version. One caveat: if two rows tie for the maximum, MATCH returns the FIRST one; and the modern XLOOKUP(MAX(...),...) form does the same job more readably. This is the standard 'who/what is the top performer' lookup."
}

{
  "slug": "how-to-use-index-match",
  "title": "How to use INDEX/MATCH",
  "task": "The portable power-lookup — works in every version, looks in any direction, survives column changes.",
  "keywords": [
    "how to use index match excel",
    "index match tutorial",
    "index match formula explained",
    "index match instead of vlookup google sheets"
  ],
  "solutions": {
    "excel": {
      "formula": "=INDEX(B2:B10,MATCH(D2,A2:A10,0))",
      "note": "MATCH finds the row; INDEX returns the value in that row of the result column. The 0 = exact match."
    },
    "google_sheets": {
      "formula": "=INDEX(B2:B10,MATCH(D2,A2:A10,0))",
      "note": "Identical."
    },
    "libreoffice": {
      "formula": "=INDEX(B2:B10,MATCH(D2,A2:A10,0))",
      "note": "Identical."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": "apple", "B2": 10,
      "A3": "banana", "B3": 20,
      "A4": "cherry", "B4": 30,
      "D2": "banana"
    },
    "formula": "=INDEX(B2:B4,MATCH(D2,A2:A4,0))",
    "expected": 20
  },
  "explanation": "Read it inside-out: MATCH(D2,A2:A4,0) finds WHICH row \"banana\" is in (row 2 of the range) using exact match (the 0), and INDEX(B2:B4, 2) returns the value in that row of the price column — 20. Because the lookup range (A) and return range (B) are named separately, INDEX/MATCH looks left as easily as right and doesn't break when you insert columns, unlike VLOOKUP's fixed column number. It's wordier than VLOOKUP but runs in every version of every app, which is why it stayed the power user's choice until XLOOKUP (which does the same thing in one call, on modern versions only)."
}

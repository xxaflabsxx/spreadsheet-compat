{
  "slug": "convert-a-column-letter-to-a-number",
  "title": "How to convert a column letter to a number",
  "task": "Turn \"AB\" into 28 — for driving INDEX, building references, or documentation.",
  "keywords": [
    "column letter to number excel",
    "convert column letter to index",
    "column number from letter",
    "letter to column number google sheets"
  ],
  "solutions": {
    "excel": {
      "formula": "=COLUMN(INDIRECT(A2&\"1\"))",
      "note": "A2 holds the column letters. INDIRECT builds a reference; COLUMN reads its number."
    },
    "google_sheets": {
      "formula": "=COLUMN(INDIRECT(A2&\"1\"))",
      "note": "Identical."
    },
    "libreoffice": {
      "formula": "=COLUMN(INDIRECT(A2&\"1\"))",
      "note": "Identical."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": "AB"
    },
    "formula": "=COLUMN(INDIRECT(A2&\"1\"))",
    "expected": 28
  },
  "explanation": "INDIRECT turns the text \"AB1\" into a real cell reference, and COLUMN returns that cell's column number — 28 for column AB. The reverse (number to letter) is the ADDRESS/SUBSTITUTE trick in the convert-a-column-number-to-a-letter recipe. INDIRECT is volatile (recalculates constantly), so for a one-off conversion it's fine, but avoid thousands of them; when you actually need to fetch data by a computed column, INDEX with the numeric result is faster and non-volatile."
}

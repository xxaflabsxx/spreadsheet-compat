{
  "slug": "count-checked-checkboxes",
  "title": "How to count checked checkboxes",
  "task": "Total the ticked boxes in a checklist column.",
  "keywords": [
    "count checkboxes google sheets",
    "countif true",
    "checklist progress formula",
    "count ticked boxes excel"
  ],
  "solutions": {
    "excel": {
      "formula": "=COUNTIF(A2:A100,TRUE)",
      "note": "Excel checkboxes (Insert → Checkbox, 365) store TRUE/FALSE in the cell."
    },
    "google_sheets": {
      "formula": "=COUNTIF(A2:A100,TRUE)",
      "note": "Identical — Sheets checkboxes are TRUE/FALSE cells too."
    },
    "libreoffice": {
      "formula": "=COUNTIF(A2:A100,TRUE())",
      "note": "Same idea; LibreOffice writes booleans as TRUE()/FALSE()."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": true,
      "A3": false,
      "A4": true
    },
    "formula": "=COUNTIF(A2:A4,TRUE())",
    "expected": 2
  },
  "explanation": "Modern checkboxes are just a checkbox-shaped view of a TRUE/FALSE cell, so COUNTIF with TRUE counts the ticked ones — 2 in the sample. Progress percent is =COUNTIF(A2:A100,TRUE)/COUNTA(A2:A100), and since TRUE sums as 1, =SUMPRODUCT(--A2:A100) counts them too. If your boxes write custom values instead (Sheets lets you set them), count those values instead of TRUE."
}

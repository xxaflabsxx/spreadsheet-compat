{
  "slug": "split-text-to-columns",
  "title": "How to split text into separate columns with a formula",
  "task": "Break a delimited string (like a comma-separated list) into separate cells, spilling across columns.",
  "keywords": [
    "split text to columns formula",
    "textsplit google sheets",
    "separate text by comma",
    "split cell excel"
  ],
  "solutions": {
    "excel": {
      "formula": "=TEXTSPLIT(A2,\",\")",
      "note": "Excel 365. Spills across columns. Second delimiter arg can split rows too."
    },
    "google_sheets": {
      "formula": "=SPLIT(A2,\",\")",
      "note": "Google Sheets uses SPLIT(); TEXTSPLIT is Excel-only."
    },
    "libreoffice": {
      "formula": "=TEXTSPLIT(A2,\",\")",
      "note": "LibreOffice 25.8 supports TEXTSPLIT."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": "a,b,c"
    },
    "formula": "=TEXTSPLIT(A2,\",\")",
    "expected": [
      "a",
      "b",
      "c"
    ],
    "check_range": "D1:F1"
  },
  "explanation": "TEXTSPLIT(text, column_delimiter) breaks the string at each delimiter and spills the pieces across columns. Google Sheets calls the same thing SPLIT(). For older Excel, use the Data > Text to Columns wizard."
}
{
  "slug": "how-to-use-xlookup",
  "title": "How to use XLOOKUP",
  "task": "The modern lookup — exact match by default, looks in any direction, and handles not-found gracefully.",
  "keywords": [
    "how to use xlookup excel",
    "xlookup tutorial",
    "xlookup formula",
    "xlookup vs vlookup google sheets"
  ],
  "solutions": {
    "excel": {
      "formula": "=XLOOKUP(D2,A2:A10,B2:B10,\"Not found\")",
      "note": "lookup value, lookup range, return range, [if not found]. Needs Excel 2021+/365."
    },
    "google_sheets": {
      "formula": "=XLOOKUP(D2,A2:A10,B2:B10,\"Not found\")",
      "note": "Identical."
    },
    "libreoffice": {
      "formula": "=XLOOKUP(D2,A2:A10,B2:B10,\"Not found\")",
      "note": "Needs LibreOffice 24.8+; earlier versions use INDEX/MATCH."
    }
  },
  "verify": {
    "setup_cells": {
      "A2": "apple", "B2": 10,
      "A3": "banana", "B3": 20,
      "A4": "cherry", "B4": 30,
      "D2": "cherry"
    },
    "formula": "=XLOOKUP(D2,A2:A4,B2:B4,\"Not found\")",
    "expected": 30
  },
  "explanation": "XLOOKUP takes the value to find, the range to search, and the range to return from as three separate ranges — so unlike VLOOKUP there's no column-counting and the lookup and return columns can be in any order (it looks left just as easily as right). \"cherry\" is found in A2:A4 and 30 is returned from the matching row of B2:B4. The optional fourth argument is the not-found result (\"Not found\" here) instead of an ugly #N/A. It also defaults to exact match, so there's no FALSE to remember. XLOOKUP needs a modern version (Excel 2021+/365, current Google Sheets, or LibreOffice 24.8+); on older ones, INDEX/MATCH does the same job."
}

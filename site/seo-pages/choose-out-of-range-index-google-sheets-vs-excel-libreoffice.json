{
  "slug": "choose-out-of-range-index-google-sheets-vs-excel-libreoffice",
  "title": "CHOOSE out of range returns #NUM! in Google Sheets, not #VALUE!",
  "meta_description": "An out-of-range index in CHOOSE errors everywhere, but Google Sheets returns #NUM! where Excel documents #VALUE! and LibreOffice executes #VALUE!. Why the error code forks and how to migrate.",
  "h1": "CHOOSE with an out-of-range index: #NUM! in Google Sheets, #VALUE! in Excel and LibreOffice",
  "functions": [
    "CHOOSE"
  ],
  "body_html": "\n<p>CHOOSE picks the <em>n</em>-th item from a list: <code>=CHOOSE(2,\"a\",\"b\",\"c\")</code> is\n<code>\"b\"</code>. Every engine agrees on that. They disagree on what happens when the index runs\noff the end of the list &mdash; <code>=CHOOSE(5,\"a\",\"b\",\"c\")</code>, where only three values are\nsupplied. Excel documents a <code>#VALUE!</code> error there, and LibreOffice returns exactly that,\nbut Google Sheets returns <code>#NUM!</code> instead. Same out-of-range call, two different error\ncodes.</p>\n\n<div class=\"quirk-box\">\n  <h3>The surprise</h3>\n  <p>The error is not the surprise &mdash; both apps correctly refuse an out-of-range index. The\n  surprise is <em>which</em> error: Excel and LibreOffice raise <code>#VALUE!</code>, Google Sheets\n  raises <code>#NUM!</code>. Any formula that inspects the specific error code (with\n  <code>ERROR.TYPE</code>, or a test for one particular error) will read a different value in each\n  app.</p>\n</div>\n\n<h2 class=\"section-title\">A minimal example</h2>\n<div class=\"table-scroll\">\n<table class=\"cases\">\n  <thead><tr><th>Formula</th><th>Excel, desktop (documented)</th><th>Google Sheets (executed 2026-08-29)</th><th>LibreOffice Calc 25.8.7.3 (executed)</th></tr></thead>\n  <tbody>\n    <tr><td class=\"formula\">=CHOOSE(5,\"a\",\"b\",\"c\")</td><td>#VALUE!</td><td>#NUM!</td><td class=\"result\">#VALUE!</td></tr>\n    <tr><td class=\"formula\">=CHOOSE(2,\"a\",\"b\",\"c\")</td><td>b</td><td>b</td><td class=\"result\">b</td></tr>\n    <tr><td class=\"formula\">=CHOOSE(2.9,\"a\",\"b\",\"c\")</td><td>b</td><td>b</td><td class=\"result\">b</td></tr>\n  </tbody>\n</table>\n</div>\n<p><code>#VALUE!</code> is the documented-expected value in our test corpus; the Google Sheets and\nLibreOffice columns are what our harness computed by recalculating the same workbook &mdash; Google\nSheets via Drive import on 2026-08-29, LibreOffice in Calc 25.8.7.3, and the out-of-range result is\nidentical in all four LibreOffice releases we test (24.2, 24.8, 25.2, 25.8). The in-range and\nfractional-index rows match everywhere: <code>2.9</code> is truncated to <code>2</code> (not\nrounded to <code>3</code>), so only the out-of-range error code forks.</p>\n\n<h2 class=\"section-title\">Why it happens</h2>\n<p>Microsoft&rsquo;s reference is explicit: &ldquo;If index_num is less than 1 or greater than the\nnumber of the last value in the list, CHOOSE returns the <code>#VALUE!</code> error value.&rdquo;\nLibreOffice follows that letter for letter. Google Sheets rejects the same input but classifies it\nas a numeric-domain problem &mdash; index 5 has no fifth argument to point at &mdash; and returns\n<code>#NUM!</code>. Both are refusing the out-of-range index; they just file it under different\nerror categories. See Microsoft&rsquo;s\n<a href=\"https://support.microsoft.com/en-us/office/choose-function-fc5c184f-cb62-4ec7-a46e-38653b98f5bc\">CHOOSE function reference</a>.</p>\n\n<h2 class=\"section-title\">How to migrate safely</h2>\n<p>The value you get back is an error in every app, so a formula that just displays the result is\nunaffected. The break happens when something downstream branches on the <em>kind</em> of error.\n<code>=ERROR.TYPE(CHOOSE(5,\"a\",\"b\",\"c\"))</code> is <code>3</code> in Excel and LibreOffice\n(<code>#VALUE!</code>) but <code>6</code> in Google Sheets (<code>#NUM!</code>), so a lookup keyed\non that number silently changes branch.</p>\n<p>Do not test for a specific error code. Either bound-check the index before calling CHOOSE:</p>\n<p><code>=IF(OR(n&lt;1,n&gt;3),\"out of range\",CHOOSE(n,\"a\",\"b\",\"c\"))</code></p>\n<p>or normalise any error to one fallback with a code-agnostic wrapper:</p>\n<p><code>=IFERROR(CHOOSE(n,\"a\",\"b\",\"c\"),\"out of range\")</code></p>\n<p><code>IFERROR</code> catches <code>#VALUE!</code> and <code>#NUM!</code> alike, so it produces the\nsame result in every engine &mdash; which is exactly why it is safer here than\n<code>=IF(ERROR.TYPE(&hellip;)=3,&hellip;)</code>, a test that only ever matches in Excel and\nLibreOffice.</p>\n\n<h2 class=\"section-title\">Check before you migrate</h2>\n<aside class=\"promo-card\">\n  <div>\n    <p class=\"promo-title\">Check your own formula</p>\n    <p class=\"promo-body\">Paste any formula into the free compatibility checker and see\n    at once whether every function in it behaves the same in Excel, Google Sheets, and\n    the current LibreOffice release.</p>\n  </div>\n  <a class=\"promo-btn\" href=\"../checker.html\">Open the free checker</a>\n</aside>\n<aside class=\"promo-card\">\n  <div>\n    <p class=\"promo-title\">Auditing a whole workbook?</p>\n    <p class=\"promo-body\">Load an .xlsx into the free Migration Audit to find every formula\n    that breaks or silently changes value in the target app. It runs entirely in your\n    browser, so the file never leaves your machine.</p>\n  </div>\n  <a class=\"promo-btn\" href=\"../audit.html\">Run the Migration Audit</a>\n</aside>\n<p><strong>A note on which Excel this is.</strong> The Excel column in the tables above is Microsoft&rsquo;s documented behaviour for <strong>desktop</strong> Excel, as recorded in our test corpus &mdash; we do not run desktop Excel, and no value in that column is a measurement. <strong>Excel for the web is a different application with its own calculation engine, and that one we do run</strong> (recalculated on OneDrive, 2026-09-01). Its measured results are published on each function&rsquo;s own page rather than in these guide tables. Because we have no desktop run to compare against, a disagreement between an Excel-web measurement and the documented column is genuinely ambiguous: it may mean the web engine diverges from the desktop one, or that the documentation is wrong about both. We do not claim to know which.</p>\n"
}

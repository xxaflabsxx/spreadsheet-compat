{
  "slug": "aggregate-function-google-sheets-vs-excel-libreoffice",
  "title": "AGGREGATE works in Excel & LibreOffice but is missing from Google Sheets",
  "meta_description": "AGGREGATE (SUM/AVERAGE/etc. that ignores errors and hidden rows) is in Excel and executes in LibreOffice, but Google Sheets returns #NAME? — it has no AGGREGATE. The Sheets workarounds with SUM(IFERROR) and SUBTOTAL.",
  "h1": "AGGREGATE is in Excel and LibreOffice (executed), but Google Sheets returns #NAME?",
  "functions": [
    "AGGREGATE",
    "SUBTOTAL",
    "IFERROR"
  ],
  "body_html": "\n<p><code>AGGREGATE</code> is Excel&rsquo;s heavy-duty aggregator: one function that can act as SUM,\nAVERAGE, COUNT, MAX, MIN, LARGE, MEDIAN and a dozen others (19 in all), with an options argument\nthat tells it to <strong>ignore error values, hidden rows, or nested SUBTOTAL/AGGREGATE results</strong>.\nIt is the clean way to total a column that contains the odd <code>#DIV/0!</code> without wrapping every\ncell in <code>IFERROR</code>. LibreOffice Calc has it too. <strong>Google Sheets does not</strong> &mdash;\nthe name is unrecognised, so the formula comes back <code>#NAME?</code>.</p>\n\n<div class=\"quirk-box\">\n  <h3>The surprise</h3>\n  <p>This isn&rsquo;t a behavioural quirk &mdash; it is a missing function. A workbook that leans on\n  <code>AGGREGATE</code> to skip errors keeps working in Excel and LibreOffice and breaks the moment it\n  is opened in Google Sheets, where every <code>AGGREGATE</code> cell turns into <code>#NAME?</code>.</p>\n</div>\n\n<h2 class=\"section-title\">A minimal example</h2>\n<p>Column A holds <code>10</code>, <code>20</code>, <code>=1/0</code> (a <code>#DIV/0!</code> error) and\n<code>40</code>. We want the total of the valid numbers, <code>70</code>, without the error poisoning it.\n<code>AGGREGATE</code> function 9 (SUM) with option 6 (ignore errors) does exactly that:</p>\n<div class=\"table-scroll\">\n<table class=\"cases\">\n  <thead><tr><th>Formula</th><th>Excel, desktop (documented)</th><th>Google Sheets (executed 2026-08-29)</th><th>LibreOffice Calc 25.8.7.3 (executed)</th></tr></thead>\n  <tbody>\n    <tr><td class=\"formula\">=AGGREGATE(9,6,A1:A4)</td><td>70</td><td>#NAME?</td><td class=\"result\">70</td></tr>\n  </tbody>\n</table>\n</div>\n<p><code>70</code> is the documented-expected value in our test corpus, and it is what our harness got\nby recalculating the workbook in LibreOffice Calc 25.8.7.3 &mdash; identical in all four LibreOffice\nreleases we test (24.2, 24.8, 25.2 and 25.8), so this is not a version gap. Google Sheets, executed the\nsame way (a formula-only workbook through Drive import, 2026-08-29), returned <code>#NAME?</code>: it has\nno <code>AGGREGATE</code> function at all.</p>\n\n<h2 class=\"section-title\">Why it happens</h2>\n<p>Excel introduced <code>AGGREGATE</code> in Excel 2010 and LibreOffice implemented a compatible\nversion; Google Sheets never added it. There is no options-driven super-aggregator in Sheets, so the two\nthings <code>AGGREGATE</code> bundles &mdash; <em>ignore errors</em> and <em>ignore hidden/filtered\nrows</em> &mdash; have to be rebuilt from other functions.</p>\n\n<h2 class=\"section-title\">How to migrate to Google Sheets</h2>\n<p>Split the job by which <code>AGGREGATE</code> option you were using.</p>\n<p><strong>To ignore errors</strong> (options 6 and 7), strip the errors before aggregating:</p>\n<p><code>=SUM(IFERROR(A1:A4,0))</code> returns <code>70</code> in Google Sheets &mdash; <code>IFERROR</code>\nmaps each error cell to <code>0</code> across the range, and Sheets evaluates it as an array inside\n<code>SUM</code>. Swap <code>SUM</code> for <code>AVERAGE</code>, <code>MAX</code> or <code>MIN</code> as\nneeded (for <code>AVERAGE</code>, map errors to <code>\"\"</code> rather than <code>0</code> so they are not\ncounted).</p>\n<p><strong>To ignore hidden or filtered rows</strong> (options 1&ndash;7), use <code>SUBTOTAL</code>, which\nGoogle Sheets <em>does</em> have: <code>=SUBTOTAL(9,A1:A4)</code> sums only the visible rows. Note that\n<code>SUBTOTAL</code> does not skip errors, so if the range can contain both hidden rows and errors you\nneed both tools: <code>=SUBTOTAL(9,ARRAYFORMULA(IFERROR(A1:A4,0)))</code>.</p>\n<p>Going the other direction &mdash; a Sheets workbook into Excel or LibreOffice &mdash; needs nothing:\nboth apps read <code>SUBTOTAL</code>, <code>SUM</code> and <code>IFERROR</code> fine, and you can adopt\n<code>AGGREGATE</code> there if you want the single-call version back.</p>\n\n<h2 class=\"section-title\">Check before you migrate</h2>\n<aside class=\"promo-card\">\n  <div>\n    <p class=\"promo-title\">Check your own formula</p>\n    <p class=\"promo-body\">Paste any formula into the free compatibility checker and see\n    at once whether every function in it works in Excel, Google Sheets, and\n    the current LibreOffice release.</p>\n  </div>\n  <a class=\"promo-btn\" href=\"../checker.html\">Open the free checker</a>\n</aside>\n<aside class=\"promo-card\">\n  <div>\n    <p class=\"promo-title\">Auditing a whole workbook?</p>\n    <p class=\"promo-body\">Load an .xlsx into the free Migration Audit to find every formula\n    that breaks or silently changes value in the target app &mdash; <code>AGGREGATE</code>\n    included. It runs entirely in your browser, so the file never leaves your machine.</p>\n  </div>\n  <a class=\"promo-btn\" href=\"../audit.html\">Run the Migration Audit</a>\n</aside>\n<p><strong>A note on which Excel this is.</strong> The Excel column in the tables above is Microsoft&rsquo;s documented behaviour for <strong>desktop</strong> Excel, as recorded in our test corpus &mdash; we do not run desktop Excel, and no value in that column is a measurement. <strong>Excel for the web is a different application with its own calculation engine, and that one we do run</strong> (recalculated on OneDrive, 2026-09-01); it computes <code>AGGREGATE(9,6,A1:A4)</code> as <code>70</code>, the same as LibreOffice. Its measured results are published on each function&rsquo;s own page rather than in these guide tables. Because we have no desktop run to compare against, a disagreement between an Excel-web measurement and the documented column is genuinely ambiguous: it may mean the web engine diverges from the desktop one, or that the documentation is wrong about both. We do not claim to know which.</p>\n"
}

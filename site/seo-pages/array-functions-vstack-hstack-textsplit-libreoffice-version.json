{
  "slug": "array-functions-vstack-hstack-textsplit-libreoffice-version",
  "title": "Which LibreOffice version for VSTACK, TEXTSPLIT, TAKE & DROP?",
  "meta_description": "VSTACK, HSTACK, TEXTSPLIT, TAKE and DROP return #NAME? in LibreOffice 24.2, 24.8 and 25.2 - our executed runs show all fourteen working only from 25.8.7.3. Full version map by wave.",
  "h1": "Which LibreOffice version do you need for VSTACK, HSTACK, TEXTSPLIT, TAKE and DROP?",
  "functions": [
    "VSTACK",
    "HSTACK",
    "TEXTSPLIT",
    "TAKE",
    "DROP",
    "CHOOSECOLS",
    "CHOOSEROWS",
    "TOCOL",
    "TOROW",
    "WRAPCOLS",
    "WRAPROWS",
    "TEXTAFTER",
    "TEXTBEFORE",
    "EXPAND",
    "FILTER",
    "LET",
    "SEQUENCE",
    "SORT",
    "SORTBY",
    "UNIQUE",
    "XLOOKUP",
    "XMATCH",
    "RANDARRAY",
    "ARRAYTOTEXT",
    "BYCOL",
    "BYROW",
    "GROUPBY",
    "MAKEARRAY",
    "MAP",
    "PIVOTBY",
    "REDUCE",
    "SCAN"
  ],
  "body_html": "\n<p>Excel&rsquo;s array-shaping functions &mdash; <code>VSTACK</code>, <code>HSTACK</code>,\n<code>TEXTSPLIT</code>, <code>TAKE</code>, <code>DROP</code> and their relatives &mdash; are the\nfunctions most likely to decide whether a modern workbook survives a move to LibreOffice Calc. They\ndo not degrade gracefully. Either the release you are running has them, in which case our executed\nruns show them computing the documented answer, or it does not, in which case every one of them\nreturns <code>#NAME?</code>: the same error you would get from a typo.</p>\n\n<p>We ran the same corpus through four LibreOffice builds &mdash; 24.2.0.3, 24.8.7.2, 25.2.0.3 and\n25.8.7.3 &mdash; and the results sort into three clean groups. Fourteen array-shaping and text-split\nfunctions arrive in 25.8.7.3 and are <code>#NAME?</code> in everything older. Nine dynamic-array\nfunctions arrived a wave earlier, in 24.8.7.2. And a further nine are still <code>#NAME?</code> in the\nnewest build we tested.</p>\n\n<div class=\"quirk-box\">\n  <h3>The surprise</h3>\n  <p>25.2 is not enough. It is tempting to assume that a 2025 release of LibreOffice has the 2022-era\n  Excel array functions, but our 25.2.0.3 run returns <code>#NAME?</code> for <code>VSTACK</code>,\n  <code>HSTACK</code>, <code>TEXTSPLIT</code>, <code>TAKE</code>, <code>DROP</code>,\n  <code>TOCOL</code>, <code>TOROW</code>, <code>WRAPROWS</code>, <code>WRAPCOLS</code>,\n  <code>CHOOSECOLS</code>, <code>CHOOSEROWS</code>, <code>TEXTBEFORE</code>,\n  <code>TEXTAFTER</code> and <code>EXPAND</code> &mdash; all fourteen. They only start evaluating in our 25.8.7.3 run. The\n  gap between &ldquo;current-ish&rdquo; and &ldquo;new enough&rdquo; is one release, and it is the\n  difference between a working workbook and a sheet full of errors.</p>\n</div>\n\n<h2 class=\"section-title\">What this means for a migration</h2>\n<p>The practical rule from our runs is short. <strong>A workbook that uses any of the fourteen\nfunctions in the first table below needs LibreOffice 25.8 or newer.</strong> On 24.2, 24.8 or 25.2\nthose formulas do not compute a different answer &mdash; they do not compute at all, and the\n<code>#NAME?</code> flows downstream into every total, chart and lookup that reads the result.\nA workbook that stops at the second table&rsquo;s nine functions (<code>XLOOKUP</code>,\n<code>FILTER</code>, <code>SORT</code>, <code>UNIQUE</code>, <code>SEQUENCE</code>,\n<code>LET</code>, <code>SORTBY</code>, <code>XMATCH</code>, <code>RANDARRAY</code>) needs 24.8 or newer. A workbook that\nuses anything in the third table has no LibreOffice release in our tested range that will run it,\nand has to be rewritten.</p>\n<p>If you are checking a single formula, paste it into the free compatibility checker linked at the\nfoot of this page; if you are checking a whole file, the Migration Audit button below it takes an\n.xlsx and flags every formula whose function is missing or behaves differently in the target app.\nFor the full per-function support table across all four builds, see\n<a href=\"../libreoffice-version-support.html\">LibreOffice Calc function support by version</a>.</p>\n\n<h2 class=\"section-title\">Wave one: fourteen functions that land in 25.8.7.3</h2>\n<p>Inputs, per case: <code>CHOOSECOLS</code> reads <code>A1:C1 = 1, 2, 3</code>;\n<code>CHOOSEROWS</code> reads <code>A1:A5 = 10, 20, 30, 40, 50</code>; <code>TAKE</code> and\n<code>DROP</code> read <code>A1:C3 = 1&hellip;9</code> in reading order; <code>HSTACK</code> reads\n<code>A1:A2 = 1, 2</code> and <code>B1:B2 = 3, 4</code>; <code>TOCOL</code>, <code>TOROW</code> and\n<code>VSTACK</code> read <code>A1:B2 = 1, 2 / 3, 4</code>; <code>WRAPCOLS</code> and\n<code>WRAPROWS</code> read <code>A1:D1 = 1, 2, 3, 4</code>; <code>EXPAND</code> reads\n<code>A1:A2 = 5, 7</code> for its row-padding case and <code>A1 = 5</code> for its column-padding\ncase; the text cases take string literals.</p>\n<div class=\"table-scroll\">\n<table class=\"cases\">\n  <thead><tr><th>Function</th><th>Formula</th><th>Excel (documented)</th><th>Google Sheets (executed 2026-08-29)</th><th>LibreOffice 24.2.0.3, 24.8.7.2 &amp; 25.2.0.3 (executed)</th><th>LibreOffice 25.8.7.3 (executed)</th></tr></thead>\n  <tbody>\n    <tr><td>CHOOSECOLS</td><td class=\"formula\">=CHOOSECOLS(A1:C1,3,1)</td><td>3, 1</td><td>3 &mdash; read-back 3, 1</td><td class=\"result\">#NAME?</td><td class=\"result\">3 &mdash; read-back 3, 1 (matches)</td></tr>\n    <tr><td>CHOOSEROWS</td><td class=\"formula\">=CHOOSEROWS(A1:A5,4,2)</td><td>40, 20</td><td>40 &mdash; read-back 40, 20</td><td class=\"result\">#NAME?</td><td class=\"result\">40 &mdash; read-back 40, 20 (matches)</td></tr>\n    <tr><td>DROP</td><td class=\"formula\">=DROP(A1:C3,1,1)</td><td>5, 6 / 8, 9</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">5 &mdash; read-back 5, 6, 8, 9 (matches)</td></tr>\n    <tr><td>EXPAND</td><td class=\"formula\">=EXPAND(A1:A2,3,1,0)</td><td>5, 7, 0</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">5 &mdash; read-back 5, 7, 0 (matches)</td></tr>\n    <tr><td>HSTACK</td><td class=\"formula\">=HSTACK(A1:A2,B1:B2)</td><td>1, 3 / 2, 4</td><td>1 &mdash; read-back 1, 3, 2, 4</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 3, 2, 4 (matches)</td></tr>\n    <tr><td>TAKE</td><td class=\"formula\">=TAKE(A1:C3,2,2)</td><td>1, 2 / 4, 5</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 4, 5 (matches)</td></tr>\n    <tr><td>TEXTAFTER</td><td class=\"formula\">=TEXTAFTER(\"a-b-c\",\"-\")</td><td>b-c</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">b-c</td></tr>\n    <tr><td>TEXTBEFORE</td><td class=\"formula\">=TEXTBEFORE(\"a-b-c\",\"-\")</td><td>a</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">a</td></tr>\n    <tr><td>TEXTSPLIT</td><td class=\"formula\">=TEXTSPLIT(\"a,b,c\",\",\")</td><td>a, b, c</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">a &mdash; read-back a, b, c (matches)</td></tr>\n    <tr><td>TOCOL</td><td class=\"formula\">=TOCOL(A1:B2)</td><td>1, 2, 3, 4</td><td>1 &mdash; read-back 1, 2, 3, 4</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 3, 4 (matches)</td></tr>\n    <tr><td>TOROW</td><td class=\"formula\">=TOROW(A1:B2)</td><td>1, 2, 3, 4</td><td>1 &mdash; read-back 1, 2, 3, 4</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 3, 4 (matches)</td></tr>\n    <tr><td>VSTACK</td><td class=\"formula\">=VSTACK(A1:B1,A2:B2)</td><td>1, 2 / 3, 4</td><td>1 &mdash; read-back 1, 2, 3, 4</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 3, 4 (matches)</td></tr>\n    <tr><td>WRAPCOLS</td><td class=\"formula\">=WRAPCOLS(A1:D1,2)</td><td>1, 3 / 2, 4</td><td>1 &mdash; read-back 1, 3, 2, 4</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 3, 2, 4 (matches)</td></tr>\n    <tr><td>WRAPROWS</td><td class=\"formula\">=WRAPROWS(A1:D1,2)</td><td>1, 2 / 3, 4</td><td>1 &mdash; read-back 1, 2, 3, 4</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 3, 4 (matches)</td></tr>\n  </tbody>\n</table>\n</div>\n<p>The <code>#NAME?</code> column is one column because all three older builds agreed: every one of\nthese cases returned <code>#NAME?</code> in 24.2.0.3, in 24.8.7.2 <em>and</em> in 25.2.0.3. Of the\nfourteen, our function dataset records <code>CHOOSECOLS</code>, <code>CHOOSEROWS</code>,\n<code>HSTACK</code>, <code>TOCOL</code>, <code>TOROW</code>, <code>VSTACK</code>,\n<code>WRAPCOLS</code> and <code>WRAPROWS</code> as documented in Google Sheets&rsquo; function list;\nit does not record <code>DROP</code>, <code>TAKE</code>, <code>TEXTSPLIT</code>,\n<code>TEXTBEFORE</code>, <code>TEXTAFTER</code> or <code>EXPAND</code> there. We have now run all fourteen in Sheets, and the executed result matches that inventory\nexactly: the eight it documents returned their values, and DROP, TAKE, TEXTSPLIT, TEXTBEFORE,\nTEXTAFTER and EXPAND returned <code>#NAME?</code>.</p>\n<p>Two caveats on the 25.8.7.3 column, both covered in detail elsewhere so we will not repeat the\ntables here. <code>TEXTAFTER</code> and <code>TEXTBEFORE</code> evaluate in 25.8.7.3 but do not\nhonour the <code>if_not_found</code> fallback argument in our runs, and\n<code>FILTER</code>&rsquo;s empty-result case reports a different error than Excel documents &mdash;\nboth are measured and explained in\n<a href=\"../guides/filter-no-results-calc-error-excel-vs-libreoffice.html\">FILTER no match: Excel #CALC! vs LibreOffice #N/A</a>.\nFor hand-rolled replacements of the two text functions, see\n<a href=\"../compare/textbefore-textafter-vs-left-mid-right.html\">TEXTBEFORE/TEXTAFTER vs LEFT, MID and RIGHT</a>.</p>\n\n<h2 class=\"section-title\">Wave two: nine functions that landed in 24.8.7.2</h2>\n<p>These are the previous generation of dynamic-array functions. They are <code>#NAME?</code> in\n24.2.0.3 only; from 24.8.7.2 onward our runs show them computing.</p>\n<div class=\"table-scroll\">\n<table class=\"cases\">\n  <thead><tr><th>Function</th><th>Formula</th><th>Excel (documented)</th><th>Google Sheets (executed 2026-08-29)</th><th>LibreOffice 24.2.0.3 (executed)</th><th>LibreOffice 24.8.7.2, 25.2.0.3 &amp; 25.8.7.3 (executed)</th></tr></thead>\n  <tbody>\n    <tr><td>FILTER</td><td class=\"formula\">=FILTER(A1:A5,B1:B5&gt;2)</td><td>c, d, e</td><td>c &mdash; read-back c, d, e (matches)</td><td class=\"result\">#NAME?</td><td class=\"result\">c &mdash; read-back c, d, e (matches)</td></tr>\n    <tr><td>LET</td><td class=\"formula\">=LET(x,5,x*2)</td><td>10</td><td>10</td><td class=\"result\">#NAME?</td><td class=\"result\">10</td></tr>\n    <tr><td>RANDARRAY</td><td class=\"formula\">=ROWS(RANDARRAY(2,3))*10+COLUMNS(RANDARRAY(2,3))</td><td>23 (shape check only &mdash; individual values are non-deterministic)</td><td>23</td><td class=\"result\">#NAME?</td><td class=\"result\">23</td></tr>\n    <tr><td>SEQUENCE</td><td class=\"formula\">=SEQUENCE(5)</td><td>1, 2, 3, 4, 5</td><td>1 &mdash; read-back 1, 2, 3, 4, 5</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 3, 4, 5 (matches)</td></tr>\n    <tr><td>SORT</td><td class=\"formula\">=SORT(A1:A5)</td><td>1, 1, 3, 4, 5</td><td>1 &mdash; read-back 1, 1, 3, 4, 5 (matches)</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 1, 3, 4, 5 (matches)</td></tr>\n    <tr><td>SORTBY</td><td class=\"formula\">=SORTBY(A1:A3,B1:B3)</td><td>y, z, x</td><td>#NAME?</td><td class=\"result\">#NAME?</td><td class=\"result\">y &mdash; read-back y, z, x (matches)</td></tr>\n    <tr><td>UNIQUE</td><td class=\"formula\">=UNIQUE(A1:A5)</td><td>1, 2, 3</td><td>1 &mdash; read-back 1, 2, 3</td><td class=\"result\">#NAME?</td><td class=\"result\">1 &mdash; read-back 1, 2, 3 (matches)</td></tr>\n    <tr><td>XLOOKUP</td><td class=\"formula\">=XLOOKUP(\"b\",A1:A3,B1:B3)</td><td>2</td><td>2</td><td class=\"result\">#NAME?</td><td class=\"result\">2</td></tr>\n    <tr><td>XMATCH</td><td class=\"formula\">=XMATCH(\"b\",A1:A3)</td><td>2</td><td>2</td><td class=\"result\">#NAME?</td><td class=\"result\">2</td></tr>\n  </tbody>\n</table>\n</div>\n<p>Inputs: <code>FILTER</code> reads <code>A1:A5 = a&hellip;e</code> with <code>B1:B5 = 1&hellip;5</code>;\n<code>SORT</code> and <code>UNIQUE</code> read <code>A1:A5 = 3, 1, 4, 1, 5</code> and\n<code>1, 2, 2, 3, 1</code> respectively; <code>SORTBY</code> reads <code>A1:A3 = x, y, z</code> with\n<code>B1:B3 = 3, 1, 2</code>; <code>XLOOKUP</code> and <code>XMATCH</code> read\n<code>A1:A3 = a, b, c</code> with <code>B1:B3 = 1, 2, 3</code>. Our dataset records\n<code>FILTER</code>, <code>LET</code>, <code>RANDARRAY</code>, <code>SEQUENCE</code>, <code>SORT</code>,\n<code>UNIQUE</code> and <code>XLOOKUP</code> as documented in Google Sheets, and does not record\n<code>SORTBY</code> or <code>XMATCH</code> there. All nine have now been executed in Sheets, including a\nfollow-up run on 2026-08-29 that writes the corpus with plain (unprefixed) function names rather\nthan Excel&rsquo;s <code>_xlfn._xlws.</code> storage form. LET, RANDARRAY, SEQUENCE, UNIQUE, XLOOKUP,\n<strong>FILTER</strong> and <strong>SORT</strong> all returned their documented values &mdash; the plain-name\nrun resolved FILTER and SORT, which an earlier prefixed run had left inconclusive because Sheets&rsquo;\nimporter did not map that storage form. Only SORTBY returned <code>#NAME?</code>, and that is a real\nresult: Google does not document SORTBY. The ninth is the\ninteresting one: our inventory does <em>not</em> record XMATCH as documented in Sheets, yet it\nexecuted there and returned <code>2</code>. That is an inventory gap the run exposed.</p>\n<div class=\"quirk-box\">\n<h3>Sheets semantic split: SORT&rsquo;s third argument</h3>\n<p>Excel&rsquo;s <code>SORT(array, [sort_index], [sort_order], [by_col])</code> takes a signed\n<code>sort_order</code> (<code>1</code> ascending, <code>-1</code> descending) as its third argument.\nGoogle Sheets&rsquo; <code>SORT(range, sort_column, is_ascending, &hellip;)</code> uses that same position\nfor a boolean <code>is_ascending</code> flag instead. Our corpus&rsquo;s <code>=SORT(A1:A5,1,-1)</code> case\n(id <code>SORT_descending</code>) is written the Excel way, intending a descending sort; in the\nplain-name Sheets run it returned <code>1</code> with a read-back of <code>1, 1, 3, 4, 5</code> &mdash;\nthe <em>ascending</em> result, because Sheets reads the nonzero <code>-1</code> as truthy for\n<code>is_ascending</code> rather than as &ldquo;descending.&rdquo; A formula copied verbatim from Excel\nto sort descending will silently sort ascending in Sheets instead. Separately, a degenerate\n<code>=SORT(A1:A1)</code> case over a single blank cell (id <code>SORT_empty_range_error</code>) returned a\nblank cell in the Sheets run, not the <code>0</code> that a blank sorts to in numeric context and that\nLibreOffice also returns &mdash; sorting a lone blank cell returns blank, not zero, in Sheets.</p>\n</div>\n<p>One wrinkle inside this wave is worth stating because it cuts against the tidy story. A separate\n<code>SORT</code> case, <code>=SORT(A1:A1)</code> over a single blank cell, returned <code>0</code>\nin 24.8.7.2, <code>#VALUE!</code> in 25.2.0.3, and <code>0</code> again in 25.8.7.3. So &ldquo;landed\nin 24.8&rdquo; describes the ordinary path; one degenerate input regressed for a release and came\nback. It is a reminder that &ldquo;supported&rdquo; is per-case, not per-function.</p>\n<p><code>RANDARRAY</code> is volatile, so unlike the other rows in this table our case does not\ncheck a literal returned value. The row above is a shape probe: <code>=ROWS(RANDARRAY(2,3))*10+\nCOLUMNS(RANDARRAY(2,3))</code> encodes &ldquo;2 rows, 3 columns&rdquo; as <code>23</code>, which is\ndeterministic even though the individual cells are not. A second case in our corpus,\n<code>=AND(MIN(RANDARRAY(10,10))&gt;=0,MAX(RANDARRAY(10,10))&lt;1)</code>, checks that every value in\na 10&times;10 spill lands in <code>[0,1)</code> rather than checking what any cell actually contains.\nBoth cases return <code>#NAME?</code> in 24.2.0.3 and their expected deterministic result\n(<code>23</code> and <code>TRUE</code>) in 24.8.7.2, 25.2.0.3 and 25.8.7.3.</p>\n\n<h2 class=\"section-title\">Still unsupported in 25.8.7.3</h2>\n<p>Nine functions returned <code>#NAME?</code> in every build we ran, including the newest. There is\nno version in our tested range where these start working, so a migration has to replace them rather\nthan wait for an upgrade.</p>\n<div class=\"table-scroll\">\n<table class=\"cases\">\n  <thead><tr><th>Function</th><th>Formula</th><th>Excel (documented)</th><th>Google Sheets (executed 2026-08-29)</th><th>LibreOffice 25.8.7.3 (executed)</th></tr></thead>\n  <tbody>\n    <tr><td>ARRAYTOTEXT</td><td class=\"formula\">=ARRAYTOTEXT({1,2,3})</td><td>1, 2, 3</td><td>#NAME?</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>BYCOL</td><td class=\"formula\">=BYCOL(A1:B2,LAMBDA(c,SUM(c)))</td><td>4, 6</td><td>4 &mdash; read-back 4, 6 (matches)</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>BYROW</td><td class=\"formula\">=BYROW(A1:B2,LAMBDA(r,SUM(r)))</td><td>3, 7</td><td>3 &mdash; read-back 3, 7 (matches)</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>GROUPBY</td><td class=\"formula\">=GROUPBY(A1:A4,B1:B4,SUM)</td><td>Documented; spilled layout varies with optional arguments, so our case is a presence probe with no fixed expected value</td><td>#NAME?</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>MAKEARRAY</td><td class=\"formula\">=MAKEARRAY(2,2,LAMBDA(r,c,r*c))</td><td>1, 2 / 2, 4</td><td>1 &mdash; read-back 1, 2, 2, 4 (matches)</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>MAP</td><td class=\"formula\">=MAP(A1:A3,LAMBDA(x,x*2))</td><td>2, 4, 6</td><td>2 &mdash; read-back 2, 4, 6</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>PIVOTBY</td><td class=\"formula\">=PIVOTBY(A1:A4,B1:B4,C1:C4,SUM)</td><td>Documented; spilled pivot layout varies with optional arguments, so our case is a presence probe with no fixed expected value</td><td>#NAME?</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>REDUCE</td><td class=\"formula\">=REDUCE(0,A1:A3,LAMBDA(a,b,a+b))</td><td>6</td><td>6</td><td class=\"result\">#NAME?</td></tr>\n    <tr><td>SCAN</td><td class=\"formula\">=SCAN(0,A1:A3,LAMBDA(a,b,a+b))</td><td>1, 3, 6</td><td>1 &mdash; read-back 1, 3, 6</td><td class=\"result\">#NAME?</td></tr>\n  </tbody>\n</table>\n</div>\n<p>All nine &mdash; <code>ARRAYTOTEXT</code>, <code>BYCOL</code>, <code>BYROW</code>,\n<code>GROUPBY</code>, <code>MAKEARRAY</code>, <code>MAP</code>, <code>PIVOTBY</code>,\n<code>REDUCE</code> and <code>SCAN</code> &mdash; returned <code>#NAME?</code> in all four builds:\n24.2.0.3, 24.8.7.2, 25.2.0.3 and 25.8.7.3. Unlike the two waves above, there is no version in our\ntested range where any of them start working. Google Sheets tells a different story for six of the\nnine: <code>MAP</code>, <code>REDUCE</code> and <code>SCAN</code> already executed and matched, and the\n2026-08-29 plain-name run resolved <code>BYCOL</code>, <code>BYROW</code> and <code>MAKEARRAY</code> from\ninconclusive to matched as well &mdash; all six run correctly in Sheets even though none of them run in\nany LibreOffice build we tested. <code>ARRAYTOTEXT</code>, <code>GROUPBY</code> and <code>PIVOTBY</code>\nremain <code>#NAME?</code> in Sheets too, and that is a real result: Google does not document any of\nthe three.</p>\n<p>Six of the nine are the LAMBDA helper family, and they have their own writeup with the rewrite\nrecipes: see\n<a href=\"../guides/lambda-map-reduce-scan-excel-vs-libreoffice.html\">LAMBDA, MAP &amp; SCAN: Excel vs LibreOffice</a>,\nwhich also covers what happens to an inline <code>=LAMBDA(x,x*2)(5)</code> call in Calc.</p>\n\n<h2 class=\"section-title\">How we measured the spilling cases</h2>\n<p>This matters for reading the tables honestly, because most of these functions return an array\nrather than a single value. Each case is written into its own sheet with no cached result, and the\nfile is recalculated by LibreOffice from scratch; the formula sits in the top-left cell of a\n<em>check range</em> recorded with the case. The single value shown in the LibreOffice columns is\nthat anchor cell &mdash; which is why <code>=VSTACK(A1:B1,A2:B2)</code> shows as <code>1</code> even\nthough its documented result is <code>1, 2 / 3, 4</code>.</p>\n<p>The harness also reads back every cell of the recorded check range and compares that list,\nflattened in reading order, against the flattened expected array &mdash; that is the\n&ldquo;read-back&rdquo; shown after each anchor value, and where it says <em>matches</em>, every\ncell in the recorded range equalled the corresponding expected value. So for these cases the honest\nclaim is: <strong>the function evaluates (no <code>#NAME?</code>), the anchor cell matches, and the\ncells inside the recorded check range match value-for-value in reading order</strong>. What we did\n<em>not</em> verify is the shape of the spill &mdash; flattening makes a 2&times;2 block and a\n1&times;4 row indistinguishable &mdash; nor anything outside the recorded range, so a result that\nspilled further than expected would not be caught. For the scalar cases\n(<code>TEXTAFTER</code>, <code>TEXTBEFORE</code>, <code>LET</code>, <code>XLOOKUP</code>,\n<code>XMATCH</code>, <code>RANDARRAY</code>) there is no range: the single returned value is what was\ncompared.</p>\n\n<h2 class=\"section-title\">How to migrate safely</h2>\n<p><strong>Pin the version, if you can.</strong> The cheapest fix for wave one is not a rewrite but a\nrequirement: install 25.8 or newer everywhere the workbook will be opened. Our runs show all fourteen\nfunctions evaluating there, so a workbook that only uses wave one and wave two functions needs no\nformula changes at all &mdash; it needs a supported release. Where you cannot control the target\nversion, treat 24.8 as the floor and rewrite wave one.</p>\n<p><strong>Rewrite wave one with classic functions.</strong> <code>=VSTACK(A1:B1,A2:B2)</code> is a\nliteral block of cells, so reference the combined range directly or copy the two pieces into one\ncontiguous block. <code>=HSTACK(A1:A2,B1:B2)</code> is the same trick sideways.\n<code>=TAKE(A1:C3,2,2)</code> and <code>=DROP(A1:C3,1,1)</code> are sub-ranges: use\n<code>=INDEX($A$1:$C$3,ROW()-ROW($E$1)+1,COLUMN()-COLUMN($E$1)+1)</code> filled across the target\nblock, or simply point at <code>A1:B2</code> and <code>B2:C3</code> when the offsets are fixed.\n<code>=CHOOSECOLS(A1:C1,3,1)</code> and <code>=CHOOSEROWS(A1:A5,4,2)</code> become one\n<code>=INDEX(range,row,col)</code> per output cell. <code>=TOCOL(A1:B2)</code> and\n<code>=TOROW(A1:B2)</code>, and the <code>WRAPROWS</code>/<code>WRAPCOLS</code> pair, are index\narithmetic: <code>=INDEX($A$1:$B$2,INT((ROW()-1)/2)+1,MOD(ROW()-1,2)+1)</code> filled down reproduces\nthe flatten, with the divisor set to the block width. <code>=TEXTSPLIT(\"a,b,c\",\",\")</code> becomes\n<code>LEFT</code>/<code>MID</code>/<code>RIGHT</code> around <code>FIND</code>, or Calc&rsquo;s\nText-to-Columns for a one-off.</p>\n<p><strong>Do not rely on catching <code>#NAME?</code>.</strong> Wrapping a missing function in\n<code>=IFERROR(VSTACK(...),\"\")</code> hides the failure instead of fixing it: the workbook opens\nclean and reports blanks where data should be, which is worse than a visible error. Fix the version\nor fix the formula.</p>\n<p><strong>Check before you commit to a target.</strong> The two buttons below are the fast path: the\nchecker answers &ldquo;does this one formula survive?&rdquo; and the Migration Audit answers\n&ldquo;which formulas in this file do not?&rdquo; across a whole .xlsx, in the browser.</p>\n\n<h2 class=\"section-title\">Honest limits</h2>\n<p>The Excel column throughout is Microsoft&rsquo;s documented behaviour as recorded in our test\ncorpus &mdash; we did not run Excel, and nothing on this page is an Excel measurement. The Google Sheets column is executed output: the same .xlsx was imported to Google Drive on\n2026-08-29, recalculated by Sheets, and exported back for readback. Sheets has no version to pin, so\nthat run is dated rather than versioned. An earlier pass stored these formulas with Excel&rsquo;s\n<code>_xlfn._xlws.</code>/<code>_xlfn.</code> storage prefix, which Google&rsquo;s importer did not map for\nfive cases (FILTER, SORT, BYCOL, BYROW, MAKEARRAY), so those cells came back inconclusive; a\nfollow-up run on 2026-08-29 that writes the same cases with plain, unprefixed function names\nresolved all five, and the values shown above are from that run. Both runs are kept in our results\nfile, with per-function provenance recorded in <code>subset_runs</code>. The LibreOffice columns are executed output, and every version claim on this page traces to\none of four result files from LibreOffice Calc 24.2.0.3, 24.8.7.2, 25.2.0.3 and 25.8.7.3. Those are\nthe only four builds we tested: a function we describe as arriving in 25.8.7.3 may have arrived in\nany release after 25.2.0.3, and we cannot narrow it further. See\n<a href=\"../methodology.html\">our methodology</a> for how recalculation is proven rather than\nassumed.</p>\n\n<h2 class=\"section-title\">Check before you migrate</h2>\n<aside class=\"promo-card\">\n  <div>\n    <p class=\"promo-title\">Check your own formula</p>\n    <p class=\"promo-body\">Paste any formula into the free compatibility checker and see\n    at once whether every function in it behaves the same in Excel, Google Sheets, and\n    the current LibreOffice release.</p>\n  </div>\n  <a class=\"promo-btn\" href=\"../checker.html\">Open the free checker</a>\n</aside>\n<aside class=\"promo-card\">\n  <div>\n    <p class=\"promo-title\">Auditing a whole workbook?</p>\n    <p class=\"promo-body\">Load an .xlsx into the free Migration Audit to find every formula\n    that breaks or silently changes value in the target app. It runs entirely in your\n    browser, so the file never leaves your machine.</p>\n  </div>\n  <a class=\"promo-btn\" href=\"../audit.html\">Run the Migration Audit</a>\n</aside>\n"
}
